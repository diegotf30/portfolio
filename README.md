# diego.tf

Personal portfolio. Astro, no runtime framework, static output to GitHub Pages.
English at `/`, Spanish at `/es/`. All copy lives in `src/i18n.ts`.

See [DEPLOY.md](DEPLOY.md) for DNS + Pages setup.

```bash
npm install
npm run dev
npm run build
```
